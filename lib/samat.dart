import 'package:flutter/material.dart';

class Samat extends StatelessWidget {
  const Samat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text(
      'Samat',
    )));
  }
}
